server {
    listen 443 ssl;
    http2 on;
    server_name _;

    ssl_certificate     /etc/ssl/arm/tls.crt;
    ssl_certificate_key /etc/ssl/arm/tls.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;

    # CSP kept strict (all 'self') with two framework-required allowances:
    #   - script-src 'unsafe-inline': SvelteKit's adapter-static SPA-fallback
    #     index.html carries inline bootstrap/hydration scripts that cannot be
    #     hash-covered (SvelteKit only stamps CSP hashes onto PRERENDERED pages;
    #     this build is a non-prerendered SPA fallback — see svelte docs / kit
    #     #9368: a hash would also disable 'unsafe-inline'). The bundle is
    #     first-party + same-origin.
    #   - style-src 'unsafe-inline': Svelte component <style>/inline styles + Tailwind.
    # Posters route through the same-origin image-proxy and the Rajdhani font is
    # self-hosted (/fonts), so img-src/font-src stay 'self' (no external hosts).
    add_header Content-Security-Policy "default-src 'self'; img-src 'self' data:; font-src 'self'; connect-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; frame-ancestors 'none'; base-uri 'self'" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer" always;

    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass https://arm-backend:8443;
        proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
        proxy_ssl_verify on;
        proxy_ssl_server_name on;
        proxy_set_header Host arm-backend;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Authorization $http_authorization;
    }

    # FastAPI listens at exactly `/ws` (no trailing slash, no sub-routes).
    # Use exact match so we don't 301 the WebSocket handshake into the SPA
    # fallback, which silently breaks live progress in the browser.
    location = /ws {
        proxy_pass https://arm-backend:8443/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_ssl_trusted_certificate /etc/ssl/certs/ca-certificates.crt;
        proxy_ssl_verify on;
        proxy_ssl_server_name on;
        proxy_set_header Host arm-backend;
        proxy_read_timeout 1d;
    }

    # In-app help bundle (built by the Dockerfile's docs stage). A missing
    # file must be a real 404, not the SPA's index.html.
    location /docs-data/ {
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
